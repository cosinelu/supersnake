# 消食蛇 · 网页版 v3.0.3（横版 · 大地图自由游动 · 在线对战 · AI对战 · 尾巴节 · 特殊道具 · 音效）

> ⚠️ **AI Agent / WorkBuddy 必读**：开始任何工作前，先读 **[`docs/README.md`](docs/README.md)（最高准则：分支与发布规则）** 与根目录 `AGENTS.md`。本文件以下是版本历史与玩法说明。

微信小游戏《消食蛇》的浏览器版。纯原生 Canvas 2D，**零依赖、零构建**。

> **v3.0.3 矮屏（手机横屏）UI 溢出治理**：手机横屏可视高度只有 ~360~390px，
> 两处**固定像素纵向堆叠**的布局会跑出屏幕底部，而 canvas 没有滚动条、拖也拖不动 → 点不到。
> ①**右侧 HUD 小地图被切掉一截**（实测 844×390 溢 70px、800×360 溢 95px）——根因不在小地图本身：
> 它上方的标题/分数/目标/排行榜/颜色格已经占掉 ~372px，小地图的可用高度是**负数**，
> 单纯缩小它治不了本。`drawPanel` 改为**流式布局**：先累计各区块理想高度，按真实可用高度
> 算紧凑系数，再按优先级丢弃低价值区块（操作提示 → 分数构成 → 颜色格），然后缩小地图、
> 最后压缩行距；**小地图作为关键信息永远完整可见**。结果：844×390 余量 62px、800×360 余量 37px，
> 桌面/大屏仍是原来的 202×135（无回归）。
> ②**菜单按钮下面几个看不到**（溢出 73~91px）——新增 `Game.solveButtonStack(n, top, bottom)`：
> 按可用高度等比压缩按钮高/间距（下限 34px 保证可点），并把整组**垂直居中**于可用区，
> 而非从固定的 `H*0.40` 硬起步；极端矮屏（320×240）额外收缩标题预留
> ——**点不到的按钮比挤一点的标题更糟**。同一规则应用于菜单/关卡选择/过关/结算四个页面。
> **刻意不做拖动滚动**：canvas 手势与摇杆、按钮冲突，且用户预期是「一屏看全」。
> 验证：用可拦截 `drawMinimap` 的假 canvas 实测 **10 种屏幕尺寸 × 7 个 UI 状态**，
> 所有按钮与整幅小地图均落在屏内；桌面菜单按钮仍精确为 54px。
> `test/net/layout.test.js` 81 → **98 条断言**（新增 T6 覆盖矮屏按钮包含性、压缩边界、
> `solveButtonStack` 契约）。全套 559 项全绿。
>
> **v3.0.2 修「头抛弃身体」+ 修摇杆输入卡死 + 移动端横竖屏适配**：
> ①**修复联机时「头抛弃身体，只有头能动」**——根因是 `js/net/prediction.js` 的 `attach`
> 把快照节心序列（间距 `SEG_SPACING`=30px）直接当轨迹用，而轨迹采样步长 `TRAIL_STEP` 仅 3px，
> 导致轨迹弧长恰好等于身体长度、**余量为零**；一旦 colors 被服务器判定暴涨（连吃/流星注入/
> 尸体色块），轨迹立刻不够，`computeBody` 沿轨迹排布时尾部全部节堆在末点。实测出生不久时
> 5 节→40 节会造成 **35/40 节堆叠、间距 0.0px**，跑 150 帧仍不恢复。修复为「按 `TRAIL_STEP`
> 细分插值 + 尾部外推」保证弧长 ≥ 需求 + 90px 余量，且 **`reconcile` 中 colors 变长后必须
> 重新校验**（这是主要触发路径，只在 `attach` 修不够）。该 bug **只有本人可见**（他机蛇走插值层
> 直接用快照节心渲染，不重建轨迹），这也是判定该现象的关键特征。顺带修复 `onlineMatch` 把预测体
> `colors`/`segPos` 按引用赋给视图蛇的污染隐患。新增 `test/net/body.test.js` 45 条断言。
> ②**修复摇杆输入永久卡死**（用户表现为「我能动，对方很难动，但她能看到画面在动」）——原为
> 「单指独占锁」，三类场景会锁死：A 按着→B 触屏被忽略→抬起 A 后 B 永不响应／倒计时按住手指
> 开局后整局锁死／`matched` 时代码强制释放摇杆导致代码态与手指物理态脱钩。现改为
> **任意在屏手指均可接管 + 未激活时收到 move 自动 latch + 接管者抬起时转交给其它在屏手指 +
> `setState('play')` 自动 latch 已按住的手指**。另修 `server/room.js` 的 seq 回退隐患
> （客户端重连后 seq 归零而服务端 `lastSeq` 停在旧值 → 该玩家输入被**永久**丢弃），
> 新增 `INPUT_SEQ_RESET_GAP` 区分「小幅乱序丢弃」与「大幅回退＝重新计数则重置基线」。
> 新增 `test/net/input.test.js` 25 条断言。
> ③**移动端横竖屏适配**：`Game.layout()` 按朝向分支——横屏右侧竖条 HUD，**竖屏改顶部横条**
> （原硬编码右侧面板会吃掉近半屏宽、可玩区退化成窄条）；返回值升级为视口矩形
> `viewX/viewY/viewW/viewH`（世界渲染与相机的唯一权威来源，`areaW` 保留为兼容别名）；
> 摇杆按朝向调整半径/边距并避让安全区（`env(safe-area-inset-*)` 经 CSS 变量暴露给 JS）；
> 重布局改由 `resize` + `orientationchange`（80/300ms 二次校正）+ `visualViewport.resize` 驱动。
> 新增 `test/net/layout.test.js` 81 条断言（7 种设备尺寸 + 旋转 + 极端尺寸 + 相机 + 安全区）。
> **不含全屏**：Fullscreen API 必须用户手势触发且 iPhone Safari 不支持，本版不做。
>
> **v3.0.1 在线对战支持单人开局**：把超时补位的最少真人数从 2 降到 1（`server/config.js` 的 `MIN_HUMANS`），
> 即**一个人点「在线对战」等 20 秒也能开局**，由 3 条 AI 补位。此前单人排队会一直卡在等待页（队列不足 2 人永不成团）。
> 服务端结算阈值原已对单真人局做特判（`room.js` 按 `totalHumans` 区分：单人局以本人死亡为终局，
> 不会开局即结算），故本次无需改判定逻辑。新增 `test/net/run-all.js` S6 单人局场景 8 条断言回归
> （含「开局后未立即结算」这条关键防回归断言）；匹配等待页文案同步改为「最多等 20 秒，之后 AI 补位开局」。
>
> **v3.0.0 在线对战正式发布 + 模式更名 + 昵称修复 + 文档重构**：①**「在线对战」正式上线**——联机匹配真人同场竞技（服务器权威模拟 + 客户端预测/插值，满 4 人即开、20 秒超时 AI 补位，掉线判负不重连），公网入口 https://snake.pippocao.top（测试环境 dev-snake.pippocao.top），CI/CD 全链路（develop push 自动部署测试环境，main 手动发布正式环境）；②**「多人对战」更名「AI对战」**——与在线对战不再混淆（主菜单按钮 / HUD / 结算副标题同步更名，代码内部模式标识仍为 `multi`）；③**修复联机昵称相同 bug**：同一浏览器多标签页共享 localStorage 会导致同名进场，现在服务器建房时强制去重（同名追加 ·2 ·3，超长截断），且 AI 昵称不再与真人撞名（真人淘汰后名字整局保留），新增 `test/net/nick.test.js` 9 条断言回归；④**文档重构**：新增根目录 `AGENTS.md` 与 `docs/README.md` 最高准则（分支与发布规则：只在 develop 开发或 feature 合入 develop；合入 main = 阶段性版本 = 升版本号 + 打 tag + 手动发布）、`docs/design/01-game-design.md`（四模式区别 + 完整游戏规则）。
>
> **v2 核心机制重构**：从「网格制 + 四方向 + 单屏地图」改为
> 「**自由方向连续移动 + 世界大地图 + 跟随镜头**」（贪食蛇大作战式），
> 吃到的色块从**头部**补入（祖玛式一端推入），HUD 新增**小地图**。
>
> **v2.1 新增多人对战模式**：玩家 1 名 + 6 条 AI 蛇同场竞技（贪食蛇大作战式），
> 撞身即亡、尸体掉色块、AI 淘汰后自动补编，右侧 HUD 带实时排行榜。
>
> **v2.2 多人追加**：**咬断机制**（撞身时被撞者也短一节，可触发消除/保底淘汰）
> 与**结算统计扩充**（新增「累计消除方块」，结算改为手绘风卡片 + 逐行统计 + 入场动画）。
>
> **v2.3 新增尾巴节**：每条蛇末尾恒有 1 节**不可消除的深色小尾鳍**——不参与消除判定、
> 不会被消除/咬断移除、不占保底计数（颜色节保底仍是 3），避免出现整条蛇被消完的极限情况；
> 出生初始 = 5 颜色节 + 1 尾巴节 = 6 节总长（重生 AI 同样）。
>
> **v2.5 试玩反馈 5 项打磨**：①边界墙加粗到 44px 并加深可见（撞即死不变）②尾巴节重做为
> 圆润深色块 + 小圆尾尖（去掉丑三角）③加速更明显（长度/时间加成系数上调，HUD 显示实时速度）
> ④关卡地图随关递增（L1 仅 1500×1000，L10 到 4800×3200）⑤新增特殊道具：
> **万能色**（白底星标，通配任意相邻同色凑 4 连）、**炸弹**（清掉所有 ≥2 连同色段）、
> **减速**（4 秒内速度 ×0.6 喘息）。
>
> **v2.8 道具补偿「中段消除」需求**：静态砖块恢复为头部插入（干净、可预期）；改用道具达成
> 大规模消除 + 可靠中段注入——
> ①**消色**（每个已解锁色都会刷到）：吃到即消除蛇身**全部该颜色**节；
> ②**后 3 消色**：吃到即消除该颜色**最靠尾的 3 节**；
> ③**随机消除 ×1/×2/×3**（三种道具）：吃到随机移除 1/2/3 节；
> ④**流星砖块**：只从**上下左右四个正方向**之一、平行于地图边、从一侧**直线匀速飞到对侧**（无归向、慢速可见），
> **命中身体即把该色注入中段**（可靠的中段注入，落地即可能触发 4 连消除与连锁），飞越整张地图后由出界回收。
> 特殊道具按权重刷新（含旧万能/炸弹/减速）。
>
> **v2.8.7 图鉴对齐 + 首页不挡字 + 动态增长 AI**：①**图鉴图标与游戏内完全对齐**：图鉴用与游戏内同一个 `drawItemBlock` 离屏渲染再贴图，图标、描边、配色与场上道具**像素级一致**；②**图鉴翻页**：道具一页放不下时按 5 个/页翻页（点击屏幕左半上翻、右半下翻），左下角「◀ 上一页 / 下一页 ▶」提示，底部「第 X / Y 页」；图鉴新增「返回」按钮，可回到主菜单；③**修复首页图鉴按钮遮住下方文字**：菜单按钮整体上移、副标题/最高分文字下移到不重叠位置，图鉴按钮独占一行且宽度收窄；④**多人对战 AI 动态增长**：开局仅 `MP_AI_START_COUNT`（默认 3）条 AI，随着存活时间每隔 `MP_AI_GROW_INTERVAL_SEC`（默认 25 秒）新增 1 条、封顶 `MP_AI_MAX_COUNT`（默认 14）条，**越到后期 AI 越多越热闹**；⑤**AI 智力有差异**：早期 AI（`smartness` 低）性格参数随机性大（greed/caution 0.4~1.6，有的莽有的怂）、速度偏低；后期 AI（`smartness` 随存活时间从 0 渐变到 1）更均衡聪明（greed 0.8~1.3、caution 0.9~1.4）且速度略快，给玩家更大压力。
>
> **v2.12.2 图鉴微调（标题页签间距 + 删颜色页副标题）**：①顶栏高度从 72→100px，标题与居中页签间距从 ~14px 拉大到 ~34px，内容区整体下移；②删除颜色解锁顺序页的副标题行「顺序 = 解锁先后 · 闯关按关卡 / 无尽按存活时间」，颜色网格直接从内容区顶部开始。

> **v2.13.0 多人专属「彩色星」抢夺道具**：在**多人对战模式**新增专属道具「彩色星」（品质=彩色，独立于强/中/弱三档、图鉴最高优先级）。①**仅多人出现**：单/无尽绝不刷新（spawner.grabEnabled 默认 false）；多人对局约 18s 后首次出现，场上**同时最多 1 颗**，被吃掉或 42s 超时后约 32s 再次出现；②**吃到立即 +20% 总分**：玩家吃到后当前总分（存活分+消除分）立即 +20% 作为一次性加成（可叠加，每次抢到再 +20%），AI 吃到只消耗道具、不加分（制造抢夺博弈）；③**屏幕上方常驻播报**：只要场上还存在彩色星，顶部就持续显示「彩色星出现！快去抢夺 +20% 分」（内嵌图标 + 呼吸闪烁，自适应窄屏）；④**小地图一圈圈涟漪**：彩色星在小地图上的位置持续扩散彩虹涟漪圈，全场一眼可见其方位；⑤**AI 也会抢**：AI 决策对彩色星有远超普通食物的吸引力，主动争抢。彩色星比普通色块更大、带彩虹环描边，图鉴已收录。

> **v2.12.1 图鉴 UI 紧凑居中重做（去侧边栏+去品质图例+单组页签+返回按钮底部居中）**：用户反馈 v2.12.0 三区框架"信息太分散、页签重复、标题没对齐"。本次改为**紧凑居中布局**——①删除左侧导航栏（`drawGuideSidebar` 整个移除），内容区左右留边距后居中占满屏幕宽度；②删除道具页左上角品质强弱图例行；③页签从"顶栏右侧 + 左侧导航栏"两组合并为**唯一一组居中页签**（标题正下方，80×32 胶囊，选中墨色填充）；④返回按钮从底栏右对齐改为**底部居中**（120×38，带 ← 箭头）。`cfg.guideTabRects()` 简化为 `{tabItems, tabColors}` 两个居中热区；`onTouchStart` 同步改用新热区。

> **v2.12.0 图鉴整体 UI 重做（完整框架，而非零散按钮）**：用户反复反馈图鉴"没有设计感、只有零散按钮"，本次把图鉴页从「拼贴式布局」彻底改为**三区应用式框架**——①**顶栏**（全宽 52px）：标题居中、"道具 / 颜色"两个胶囊页签靠右对齐；②**左侧导航栏**（带蜡笔底色的面板 + 圆角）："道具 / 颜色"两个导航项，选中项用墨色填充 + 橙色竖条高亮；③**内容区** + **底栏**（全宽 52px）：道具页为品质图例 + 5 个/页的手绘卡片，颜色页为 4×2 共 8 色卡片网格（角标 + 蜡笔块 + 中文名 + 闯关/无尽解锁条件）；底栏左侧"上一页/下一页"提示、靠近中左"第 X / Y 页"、右对齐"返回"按钮（与自绘位置精确对齐、可点击返回主菜单）。`cfg.guideTabRects()` 重写为 `{hdrItems,hdrColors,sideItems,sideColors,contentX}` 结构；`onTouchStart` 的图鉴分支同步改用新热区（顶栏胶囊 + 左侧导航栏都支持切换），原 `drawGuide` 不再调用 `drawButtons`，返回按钮改为在底栏自绘并通过 `buildButtons` 同坐标热区命中。

> **v2.11.4 图鉴新增「颜色解锁顺序」页签**：在道具图鉴顶部新增「道具 / 颜色」两个页签——「颜色」页按 `COLOR_KEYS` 顺序（即解锁先后顺序）展示全部 8 种砖块颜色，每种给出中文名、序号角标、蜡笔色块，以及**闯关模式**（第 N 关解锁）与**无尽模式**（存活 N 秒解锁）两种解锁条件；数据全部来自纯函数 `cfg.colorUnlockPlan()`，与游戏内解锁逻辑严格一致。同时修复了图鉴「返回」按钮此前不渲染的问题（`drawGuide` 末尾调用 `drawButtons` 正常画出）。

> **v2.11.3 消除保底改为「整段消除 + 随机补砖继续」**：原保底行为（消除会清空到不足 3 节时，从尾部取消标记、保留 3 节旧块继续走）改为——**触发保底时整段消除，随后随机补满 `REFILL_ON_FLOOR`(默认 4) 块「已解锁颜色」砖继续游戏**（保留残余的非消除节；补砖避免与前 3 块凑成 4 连同色，防止下一帧反复清空）。场景：只剩 3 节同色又吃到第 4 节触发消除时，现在会整段清掉并随机补 4 块，而不是滞留 3 节同色。注意此改动**仅作用于标准 4 连消除**（`eliminate(MIN_LENGTH, ELIM_RUN)`），**不影响炸弹等 2 连消除道具**的原有保底。

> **v2.11.2 标题动效改为「向左走 + 每种颜色轮流演示」**：把 v2.11.1 的"蛇头固定在右端、只有当前色在头侧堆 4 块"改为更直观的演示——**蛇头偏左、身体向右铺开并以 sin 波浪摆动表现"一直往前走"**；砖块自左侧流入、到蛇头被「吃下」并 unshift 补头，同色节在头侧累积；凑够 4 连即消除。颜色严格按 `COLOR_KEYS` 顺序循环（红→蓝→绿→橙→紫→黄→青→粉→红…），**每种颜色喂满 4 块走一次消除再换下一色**，常驻蛇身保持多色不会空场，看起来就是"不停往前走、每种颜色都来一次"地循环。状态挂在 renderer 实例（this.titleFx），复用 game.particles 在菜单态渲染。

> **v2.11.1 标题动效还原「吃砖块→补头→4连消除」游戏过程**：把 v2.11 的"整波滑入即爆"改为更贴近玩法的演示——标题下方一条轨道上有一个带眼睛的蛇头（固定在右端，蛇身向左铺开），色块从右侧一块一块滑入被「吃下」、补到头部（与游戏内 unshift 一致），同色节在头侧累积；凑够 4 连即触发消除（蜡笔屑迸裂 + 高亮环 + 星形闪光 + 飘字「4连消除！」/「5连！」），随后循环。颜色按 COLOR_KEYS 轮播覆盖全部 8 色，偶尔先扔一块杂色展示"混色"。状态挂在 renderer 实例（this.titleFx），复用 game.particles 在菜单态渲染。

> **v2.11 标题动效改为「滑动→消除」循环**：主菜单标题「消食蛇」下方不再只做往返滑动，改为**色块从左侧沿轨道滑入 → 到达中央触发消除特效（蜡笔屑迸裂 + 高亮环 + 星形闪光 + 飘字）**的循环动画。颜色按全部 8 色调色板依次轮播（偶尔随机），每波块数 3/4/5 随机、飘字在「消除！/4连！/连锁！/暴击！/完美消除！」中随机，看起来像各种颜色都在被消除。状态挂在 renderer 实例（this.titleFx），复用 game.particles 在菜单态渲染。

> **v2.10.3 标题动效修正**：主菜单标题「消食蛇」下方原随机抖动的碎屑换成**少量砖块沿线平滑往返滑动**（sin 振荡，不随机跳动，不再像 bug），保留红/绿/蓝三字各自着色与橙色波浪下划线。
>
> **v2.10 多人飘字 + 自吃动效 + 流星/道具补充规则 + 稀有度分级**：①**多人对战道具飘字**：此前多人模式吃道具不弹效果说明（且特殊道具被当成普通色块处理），现已与单人一致——吃到 wild/bomb/slow/clear/clear3/rand1-3 真正触发对应效果，并仅对玩家弹出效果飘字；②**蛇头自吃（碰自身吃砖块）动效**：接触点加咬合环 + 牵引线拉向蛇头（带箭头）+ 头部迸裂 + 飘「自吃 +1」文字，一眼看出触发了"吃"而非飘过；③**流星砖块 + 道具补充规则**：修复多人模式从不更新流星（现与单人一致，流星会在多人场飞入）；特殊道具刷新概率随存活时间线性爬升（`ITEM_SPECIAL_CHANCE_MIN` 0.12 → `ITEM_RAMP_SEC` 180s 后 `MAX` 0.42，越后期道具越多）；**阵亡掉落**：任意蛇阵亡除身体散落色块外，**必然额外掉 1 个随机道具**，且每有 10 个颜色节（不含尾巴节）再额外掉 1 个随机道具（长度越长奖励越多）；④**道具稀有度分级**：按强度分 3 档，边框由强到弱为 **橙 / 紫 / 蓝**（明显加粗描边），图鉴按稀有度排序并展示稀有度徽标 + 顶部图例。
>
> **v2.9 自给自足消除 + 消除分占比提升 + AI 更聪明**：①**新增「蛇头碰自身吃砖块」机制**：蛇头游动中触碰到自己身体时（非撞死判定），会从身体里「咬」下一个砖块补到头部——**总长度不变但把身体里的一个砖块吃到头部**，制造额外凑 4 连 / 连锁的机会（受 `SELF_PULL_CD` 380ms 冷却限制，避免每帧反复触发）；②**结算消除分占比提高 + 连击倍率**：`ELIM_SCORE` 单节基础分 5→10（消除分在总得分中占比明显更高），并新增**窗口连击**机制——在 `ELIM_COMBO_WINDOW`（2400ms）内连续触发消除，每次消除倍率 +1，封顶 `ELIM_COMBO_MAX`（5 倍），连击时屏上弹出「N连击 ×N」提示，多次消除分数成倍叠加；③**多人对战 AI 更聪明**：采样方向 16→24（转向更细腻），食物判定新增「同色连续节数」奖励（`AI_RUN_BONUS`），墙/他蛇躲避惩罚加大且远点也参与（`AI_WALL_PENALTY_FAR`/`AI_SNAKE_PENALTY`），新增**自避**（`AI_SELF_AVOID`/`AI_SELF_PENALTY`）与**正面撞头**专门惩罚（`AI_HEADON_PENALTY`），惯性略降（`AI_INERTIA` 0.35→0.30）让 AI 更敢变向；单人/多人共用同一套更聪明的决策。
>
> **v2.8.6 道具不叠墙 + 图鉴 + 音效 + 刷新降频 + 流星变速**：①**修复道具生成在墙体上**：`pointInWall` 避让 padding 从 18px 加大到 34px，新增边界墙带避让检查（`WALL_THICK+BLOCK_RADIUS+10`），道具不再叠在墙上；②**主菜单新增「图鉴」按钮**：点击进入道具图鉴页，展示全部 10 种道具（普通色块/万能色/炸弹/减速/消色/后三消色/随机消1~3/流星砖块）的图标、名称和效果说明；③**音效系统**（Web Audio API 振荡器合成，零外部文件）：吃色块短音、特殊道具上扬双音、消除爆裂+连锁音阶、撞墙低沉音、解锁三连上升音、按钮点击音 + 柔和背景音乐 drone 循环（可静音）；④**道具刷新频率降低**：间隔 4s→5.5s、特殊概率 30%→18%，地图不再那么密集；⑤**流星速度随机化**：每颗流星速度 `METEOR_SPEED × (0.5~1.3)`，有的快有的慢。
>
> **v2.8.5 边界墙可见 + 道具提示 + 消除高亮**：①**边界墙真正可见**：根因是相机被严格钳制在世界内 `[0, W-vw]`，画在世界外侧的边界墙带永远在视口外被裁剪——修正 `clampCam` 两侧各放开 `WALL_THICK` 余量，蛇靠近边缘时边界墙（与内部墙同一蜡笔排线效果）即可进入视野；②**特殊道具效果提示**：吃到万能色/炸弹/减速/消色/后3消色/随机消N 时，屏幕顶部居中弹出手绘描边文字说明效果（约 1.6s 淡出），一眼看懂每个道具做什么；③**消除高亮特效**：每次消除在被消除节的位置生成「扩张环 + 中心 ✕」，尤其非连续消除（消色/随机消除散落各处的节）也能清楚标出哪些被消掉了。
>
> **v2.8.4 边界墙统一 + 动态墙体**：①四周边界墙改为**与内部墙同一套蜡笔排线画法**（灰底斜线 + 深色描边，统一可读"是墙"）；②新增**随时间自动生成障碍墙**（`WALL_SPAWN_INTERVAL_MS` 间隔、上限 `WALL_SPAWN_MAX` 段），地图随时间越来越复杂，生成时自动避开所有蛇身。
>
> **v2.8.3 美术与可读性修复**：①边界墙从「斜线排线灰带」改为**明确灰砖墙**（错缝砖纹 + 粗黑外框），一眼可读「墙不能碰」；
> ②新道具图标放大重画——消色改为 12 道粗放射线 + 外环、后 3 消色大号「×3」、随机消 N 居中大号数字 + ? 角标，缩小也看得清；
> ③白底道具（万能色 / 随机消 N）统一加**粗黑灰描边**，浅色 / 白色场景不再隐形。
>
> **v2.8.1 调优与修复**：①**边界墙复用内部墙画法**（四周一圈与内部墙同一蜡笔排线效果，一眼可读"是墙"）；
> ②**道具出现率大幅提高**（特殊道具概率 14%→30%，消色/后3/随机权重上调）；
> ③**流星更频繁**（间隔 9s→3.5s、同屏上限 3→6、出生距缩短），并改为**移动彩色砖块**（带淡拖尾，非流星光球）；
> ④**万能色砖块加粗黑描边**（白底在浅色场景也清晰可见）；
> ⑤`index.html` 所有脚本加 `?v=` 版本号，**强制浏览器刷新最新 JS**（避免改动后仍加载旧缓存看不到新内容）。
>
> **v2.8.2 手感与道具再调**：①**转向速率翻倍**（TURN_RATE 4.5→9.0 rad/s），快速变向可拐进相邻行/列砖块，
> 不再"怎么按都拐不过去"；②**流星改慢 + 守四正方向**（速度 330→140、间隔 3.5s→6.5s、同屏 6→3、去掉归向，
> 只从上下左右直飞对侧）；③**新道具权重再上调**（消色/后3 各 0.22→0.30、随机 ×1-3 提至 0.13/0.13/0.15，
> 万能/炸弹/减速下调让路），更容易遇到消色/后3消色/随机消除这几类。

## 怎么玩

**直接双击 `index.html` 即可游玩**（支持 `file://` 协议直开，无需起服务器）。
也可以 `python3 -m http.server` 后访问，效果相同。

- **桌面端**：方向键 / WASD 控制转向（支持组合键 8 方向，如 W+D = 右上）；
  也可以鼠标按住左下角摇杆拖动，**拖动方向即目标方向**（自由角度，不量化）。
- **触屏端**：按住左下角虚拟摇杆拖动（向量方向即目标角，带死区）。
- 蛇头会以**最大转向速率**平滑转向你指的方向，松开输入则保持当前方向直行。
- 建议横屏体验（竖屏也能玩，顶部会有提示条）。

### 规则

- **自由方向贪食蛇**：蛇头连续移动、恒速前进；身体沿蛇头轨迹按固定弧长间距跟随。
  不做蛇身自碰；撞到世界边界或内部灰墙立即结束。
- **节清晰可辨**：节间距 30px = 节直径 26px + 4px 纸色间隙，节描边加粗到 3px 深色，
  相邻节不重叠，连续同色有几节一眼能数清（消除游戏的核心可读性）。
- **吃色补头**：吃到或身体压到色块，该颜色 `unshift` 进头部——头立即变成新颜色，
  原头变成第 1 节身体，总长 +1（凑 4 连主要发生在头附近，这正是设计意图）。
- **尾巴节（v2.3）**：颜色序列 `colors` 保持纯颜色节，身体几何总长 = 颜色节数 + 1，
  末尾恒多 1 节「尾巴节」——画成深色（#3A3238）蜡笔小尾鳍/圆锥（蝌蚪尾巴），一眼认出"这节消不掉"。
  它不参与颜色序列的消除判定（等效于在序列末尾但不凑连）、不会被消除或咬断移除、
  不占「保底 3 节」计数；但它是身体的一部分：收集色块、撞墙/被撞判定对它正常生效
  （别人头撞上尾巴照样算撞身体）。因此全消除极限情况下蛇至少剩 尾巴 + 3 颜色节 = 4 节。
- **相邻 ≥4 连同色立即消除**：长度保底 3 节（只计颜色节），带蜡笔屑 + 星形闪光特效。
  **连锁倍率计分**：消除后颜色接拢又凑成 ≥4 连则连锁升级（chain+1），
  第 chain 次消除每节得分 = 5 × chain（chain1=+5、chain2=+10、chain3=+15…）；
  特效逐级放大（粒子数/星形大小 ×1.6、×2.2…），chain≥2 时在消除点弹手绘风「N连锁！」文字。
- 计分 = 存活秒数（1 秒 1 分）+ 消除分。
- **局内动态加速**（只改速度，几何判定不变）：
  闯关 速度 = 关卡基础 + 1.2×当前节数 + 0.5×存活秒，封顶 基础+120；
  无尽 保留时间提速（+1.5 px/s²）再叠加 1.2×节数，封顶 340 px/s。
- **世界大地图**：闯关 L1 ≈ 2400x1600 → L10 ≈ 5200x3600 世界像素；无尽 3600x2400。
  相机平滑跟随蛇头并钳制在世界内；右侧 HUD 有**小地图**（世界边界/墙/色块/视口框/蛇头亮点）。
- **边界即墙**：世界四周是与内部墙一致的蜡笔排线墙带（灰底+斜线排线+深色描边，厚 30px），
  小地图边界同步画成墙样式；撞界判定不变（碰到即死）。
- 色块每 4 秒稀疏补足（目标数 = 世界面积/90000，夹取 [8,40]；拒绝采样：距已有色块 ≥140px、
  离蛇 ≥80px、不压墙、最多试 50 次），未吃掉的保留。
- **颜色解锁**：8 色蜡笔调色板，初始 4 色（红蓝绿橙）；闯关每 2 关 +1 色，无尽每存活 45 秒 +1 色，
  封顶 8 色；新颜色解锁时弹**无底板**手绘提示（深色描边文字 + 新色小色块，浮于视口上方 1/4 处）1.5 秒。
- **闯关模式**：10 关，世界尺寸/墙段/基础速度递增，目标分 60→240；进度存 localStorage，主菜单可选关。
- **无尽模式**：速度随时间与长度提升（封顶 340 px/s），最高分存 localStorage。

### AI对战（v2.1 引入，v3.0.0 前称「多人对战」）

- **入口**：主菜单第三个选项「AI对战」（本地对 AI，无需服务器；联机真人请用「在线对战」），进入 4200x2800 共享大地图（`MULTI` 可调）。
- **场上构成（v2.8.7 动态增长）**：开局 **玩家 1 名 + `MP_AI_START_COUNT`（默认 3）条 AI 蛇**，
  随着存活时间每隔 `MP_AI_GROW_INTERVAL_SEC`（默认 25 秒）自动新增 1 条、封顶 `MP_AI_MAX_COUNT`（默认 14）条
  ——**越到后期 AI 越多越热闹**，且不影响性能（封顶可控）。每条蛇独立实例：
  自己的颜色序列 / 轨迹 / 速度档位 / 计分，复用同一个 Snake 类与蜡笔渲染。
  AI 有随机中文昵称（14 个名字池，场上不重复）、基础速度 130~170 px/s 随机；
  **智力随时间提升**：早期 AI（smartness 低）性格参数随机性大
  **greed（贪食 0.4~1.6）/ caution（谨慎 0.4~1.6）**（有的莽有的怂）、速度偏低；
  后期 AI（smartness 随存活时间 0→1 渐变）更均衡聪明
  **greed 0.8~1.3 / caution 0.9~1.4** 且速度略快，给玩家更大压力。
- **AI 行为**：每帧在 16 个候选方向上加权打分取最优——① 寻食（方向对齐 × 距离衰减，
  偏好与头部同色的色块）；② 避墙（近 ≈1 身位 / 远 ≈2 身位两个前景点探测，近点重罚、远点轻罚）；
  ③ 避蛇（前景点附近出现其他蛇身体节则避让）；④ 惯性 + 小幅随机游走。
  **AI 只输出目标角，转向速率与玩家一样受 TURN_RATE 钳制**，公平。
- **淘汰**：头撞墙/边界 → 淘汰；**头撞其他蛇身体节 → 淘汰**（身体主人记 1 击杀）；
  **头对头相撞 → 两条都淘汰**（不记击杀，简单公平）。不做蛇身自碰（沿用现有设计）。
- **咬断机制（v2.2）**：A 头撞 B 身体时，B（被撞者）**被咬中的那一节也短掉**——
  被咬节 = B 身体**颜色节**（不含头、不含尾巴节）中离 A 头心最近者，从颜色序列移除后
  身体由轨迹自然收拢；只撞上尾巴节时撞者照样淘汰，但不产生咬断（尾巴节咬不掉）；
  **保底**：被咬后 B 节数 < 3 → B 直接淘汰（尸体照常掉落，计入 A 的击杀）；
  **消除联动**：被咬移除后颜色接拢凑成 ≥4 连同色 → 正常触发消除（连锁/倍率计分照常，
  分数与「累计消除方块」都记 B）；被咬处迸小粒子 + B 短暂闪白/抖动 0.3 秒。
  AI 与玩家一视同仁（AI 咬玩家、玩家咬 AI、AI 互咬都生效）。
- **尸体掉落**：被淘汰的蛇按节在原地散落成色块（每隔 2 节掉 1 个、起点随机 ≈50% 的节），
  供其他蛇捡食——多人模式的主要资源循环。
- **补充**：AI 淘汰后延迟 3~5 秒在世界边缘、远离其他蛇的安全位置重生
  （初始 5 颜色节 + 1 尾巴节 = 6 节总长），维持 6 条 AI 编制；玩家淘汰不重生，进结算。
- **玩家体验**：相机始终跟随玩家；小地图显示所有蛇亮点（AI 用各自头部颜色、玩家金色高亮）；
  右侧 HUD 实时排行榜（按当前节数降序，玩家加粗高亮）；头顶昵称小手绘标签（深色描边无底板，
  玩家为「我」）。
- **结算（v2.2 美化）**：手绘风卡片底板（抖动描边圆角 + 纸张噪点），标题按名次变化
  （第 1 名「冠军！」+ 皇冠/星星涂鸦，2~3 名「很棒！」，其余「再接再厉」）；
  **每行一项**：排名 / 存活时间 / 总分 / 时间分 / 消除分 / **累计消除方块**（本局消除
  移除的总节数，含连锁与被咬触发）/ 击杀数 / 最终节数——左侧涂鸦小图标 + 标签 +
  右侧醒目数值；卡片淡入 + 各行依次 ~80ms 滑入；按钮（再来一局 / 返回菜单）手绘风加粗。
  最佳成绩（最长节数 + 最高分）存 localStorage 新 key `crayon_snake_web_mp_best`。
- **一致性**：消除特效 / 连锁文字 / 颜色解锁（按存活时间，规则同无尽）全部复用；
  排线边界墙 + 6 段内部墙（比无尽略少）；色块稀疏刷新密度提高
  （目标数 = 面积/70000，夹取 [10,50]，7 条蛇在吃）。

## 工程结构

```
index.html      内联 CSS + 按依赖顺序的 <script> 标签（无 ES Module，file:// 直开）
js/config.js    调色板、全部数值参数、解锁规则纯函数、多人/AI 参数   ★调参只改这里
js/utils.js     确定性伪随机 hash2、clamp、角度归一化/转向逼近
js/storage.js   localStorage 封装（JSON 容错 + 内存降级）
js/levels.js    10 关曲线：世界尺寸/墙段/目标分/蛇速（纯函数）
js/walls.js     世界边界 + 内部墙生成（一字/L/2x2 矩形组合，≤8% 面积，安全区）
js/snake.js     连续移动/转向速率钳制/trail-following/头部插入生长/4 连消除连锁保底（带连锁等级标记）/
                咬断移除节（removeSegAt，颜色序列去项、身体由轨迹收拢）/
                尾巴节（几何恒多 1 节：totalLength = colors+1，不可消除/不可咬断）
js/spawner.js   世界坐标稀疏刷新（只刷已解锁色；多人模式避让所有活蛇）
js/particles.js 蜡笔屑 + 星形闪光（连锁逐级放大）+ 「N连锁！」文字
js/ai.js        多人 AI 加权转向决策（寻食/避墙/避蛇/惯性游走，性格参数化）
js/multiplayer.js 多人对战编排：蛇管理/淘汰（墙·头身·头头）/咬断（保底淘汰·消除联动）/
                尸体掉落/AI 补充/排行榜/击杀/累计消除方块
js/renderer.js  蜡笔风渲染：纸张噪点、相机变换、抖动描边、排线边界墙、HUD 面板、排行榜、
                小地图（多蛇亮点）、AI 蛇与昵称标签、摇杆、被咬闪白抖动、多人结算卡片、
                尾巴节深色小尾鳍（drawTail）
js/joystick.js  固定底座摇杆（触屏+鼠标，自由角度）+ 键盘（8 方向组合）
js/game.js      状态机/连锁倍率计分/动态速度/关卡流/解锁/相机跟随钳制/横版布局/多人接入
js/main.js      启动、DPR 适配、resize、RAF 主循环
test/smoke.js   node 逻辑冒烟验证（121 项断言，含多人 3000 帧模拟、咬断机制与尾巴节规则）
```

所有 JS 均为 IIFE 挂 `window.CS` 全局命名空间；逻辑模块（config/utils/storage/levels/walls/snake/spawner/particles/ai/multiplayer/game）不依赖 DOM，可在 node 中直接加载。

## 验证

```bash
# 语法检查
for f in js/*.js; do node --check "$f"; done

# 逻辑冒烟（头部插入/消除连锁保底/连锁倍率计分/节可读性几何/动态速度公式/
#           转向速率钳制/轨迹间距/相机钳制/世界墙约束/spawner 间距与解锁色/解锁档位/
#           4000 帧随机游走；多人：AI 角度合法与转向限速/撞蛇淘汰/尸体掉落≈半/
#           AI 补充编制恒定/排行榜排序/玩家结算/3000 帧 1 玩家+6AI 模拟；
#           咬断：节数-1 且正确节移除/咬后 4 连消除计分给 B/保底淘汰记击杀/累计消除方块；
#           尾巴节：初始总长=颜色节+1/极端消除保底 3+尾巴恒在/咬断排除尾巴节/撞尾巴照样淘汰）
node test/smoke.js
```

## 怎么调参

只改 `js/config.js`：

| 想调什么 | 改哪个常量 | 当前值 |
|---|---|---|
| 基础蛇速 / 无尽提速 / 无尽上限 | `SNAKE_SPEED` / `ENDLESS_SPEEDUP_PER_SEC` / `SPEED_MAX` | 150 px/s · +1.5 · 340 |
| 动态加速：长度/时间加成、闯关封顶 | `SPEED_LEN_COEF` / `LEVEL_SPEED_TIME_COEF` / `LEVEL_SPEED_CAP_ADD` | +1.2/节 · +0.5/s · 基础+120 |
| 转向手感（越大越灵活） | `TURN_RATE` | 4.5 rad/s（≈258°/s） |
| 节半径 / 节间距 / 节描边 | `SEG_RADIUS` / `SEG_SPACING` / `SEG_STROKE` | 13 px / 30 px（直径26+4间隙）/ 3 px |
| 撞墙宽容度 | `HEAD_HIT_RADIUS` | 10 px |
| 相机跟手程度 | `CAMERA_LERP` | 5.0 /s |
| 色块大小 | `BLOCK_RADIUS` | 12 px |
| 消除门槛与得分 | `ELIM_RUN` / `ELIM_SCORE` / `MIN_LENGTH` | 4 · 5 · 3（保底只计颜色节） |
| 出生颜色节数 / 尾巴节颜色 | `START_LENGTH` / `TAIL_COLOR` | 5 节（+1 尾巴节=6 节总长）· #3A3238 |
| 连锁特效放大 / 文字弹出门槛 | `CHAIN_FX_STEP` / `CHAIN_TEXT_MIN` | 0.6/级（×1.6、×2.2…）· chain≥2 |
| 色块密度与间距 | `BLOCK_AREA_DIV` / `BLOCKS_MIN/MAX` / `BLOCK_MIN_DIST` / `BLOCK_SNAKE_DIST` | 90000 · [8,40] · 140px · 80px |
| 解锁节奏 / 横幅时长 | `LEVEL_UNLOCK_STEP_LEVELS` / `ENDLESS_UNLOCK_INTERVAL_SEC` / `UNLOCK_BANNER_MS` | 2 关 · 45 秒 · 1.5s |
| 墙壁密度/单元/间隙 | `WALL_MAX_RATIO` / `WALL_UNIT` / `WALL_GAP` | 8% · 48px · 60px |
| 边界墙厚度（纯视觉） | `WALL_THICK` | 30 px |
| 无尽世界尺寸 | `ENDLESS.W / H` | 3600 x 2400 |
| 多人世界尺寸 / 内部墙段数 | `MULTI.W / H / wallSegments` | 4200 x 2800 · 6 段 |
| AI 数量 / 出生节数 / 重生延迟 | `MP_AI_COUNT` / `MP_START_LENGTH` / `MP_RESPAWN_MIN~MAX_MS` | 6 条 · 5 节 · 3~5 秒 |
| AI 基础速度档位 | `MP_AI_SPEED_MIN / MAX` | 130~170 px/s 随机 |
| 尸体掉落密度 | `MP_CORPSE_STRIDE` | 每 2 节掉 1 个（≈50% 节） |
| 多人色块密度 | `MP_BLOCK_AREA_DIV` / `MP_BLOCKS_MIN/MAX` | 面积/70000 · [10,50] |
| AI 出生带 / 净距 | `MP_SPAWN_EDGE_MIN/MAX` / `MP_SPAWN_CLEAR` | 距边界 90~260px · 离蛇 ≥320px |
| 咬断保底节数 / 被咬反馈时长 | `MP_BITE_MIN_LENGTH` / `MP_BITE_FLASH_MS` | 3 节 · 0.3 秒 |
| AI 决策：候选方向 / 感知范围 | `AI_DIRS` / `AI_FOOD_RANGE` | 16 向 · 850px |
| AI 决策：同色偏好 / 避障探测 | `AI_SAME_COLOR_BONUS` / `AI_PROBE_NEAR / FAR` | ×1.6 · 70 / 150px |
| AI 决策：罚分与游走 | `AI_WALL_PENALTY(_FAR)` / `AI_SNAKE_PENALTY` / `AI_INERTIA` / `AI_WANDER` | 120(36) · 80 · 0.35 · 0.3 |
| AI 昵称池 | `AI_NICKNAMES` | 14 个中文昵称 |
| 配色 | `COLORS` / `INK` / `PAPER` | 8 色蜡笔 |

关卡曲线（世界尺寸/墙段/目标分/蛇速）在 `js/levels.js` 的 `levelConfig()`。

## 手感参数速查

| 参数 | 值 |
|---|---|
| 初始速度 | 150 px/s（闯关基础 L1→L10：150→231，局内 +1.2/节 +0.5/s 封顶基础+120；无尽 +1.5/s² +1.2/节，封顶 340） |
| 最大转向速率 | 4.5 rad/s ≈ 258°/s（转向半径 ≈ 速度/4.5，150px/s 时 ≈33px） |
| 节半径 / 节间距 / 节描边 | 13 px / 30 px（弧长，= 直径26 + 4px 间隙）/ 3 px 深色 |
| 色块半径 | 12 px（收集判定 = 节半径 + 块半径 = 25 px 圆形重叠，未随节间距改动） |
| 连锁计分 | 第 chain 次消除每节 = 5 × chain 分（chain2=+10、chain3=+15…），特效 ×(1+0.6×(chain-1)) |
| 世界尺寸 | L1 2400x1600 → L10 5200x3600；无尽 3600x2400 |
| 刷新密度 | 世界面积/90000，夹取 [8,40]，每 4 秒补足 |
| 相机 | 指数趋近 lerp 5.0/s，钳制在世界边界内 |
| 边界墙 | 视觉厚度 30 px 排线墙带（撞界判定不变，碰到即死） |
